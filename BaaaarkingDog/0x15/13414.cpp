#include <bits/stdc++.h>
using namespace std;
unordered_map<string, int> um;
map<int, string> m;
string sl[500010];

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int K, L;
    cin >> K >> L;
    for(int i = 0; i < L; i++) {
         string name;
         cin >> name;
        //  if()
       
    }

   
}