#include <bits/stdc++.h>
using namespace std;


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int direction = 0;
    int cmd = 0;
    for(int i = 0; i < 10; i++) {
        cin >> cmd;
        if(cmd == 1)  cmd -=1;
        else if(cmd == 2)
    }

}