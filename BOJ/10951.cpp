#include <bits/stdc++.h>
using namespace std;


int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    while(1) {
        int a, b;
        cin >> a >> b;
        if(cin.eof()) break;
        cout << a + b << '\n';
    }

}